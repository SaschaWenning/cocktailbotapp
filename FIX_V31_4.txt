CocktailBot V31.4 – Compile Fix

GitHub Actions error:
  Error: The getter 'primaryColor' isn't defined for the type 'AppColorThemeConfig'.

Cause:
  The backup information panel introduced three references to:
    store.appColors.primaryColor

Fix:
  Replaced all three with the existing theme getter:
    store.appColors.accentColor

This is a compile-only fix. All V31.3 features remain unchanged.
